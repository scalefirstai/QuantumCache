# Bootstrap-script dependencies. Production services (M1+) get a real pyproject.toml.
# Pinned to versions installed 2026-05-09; bump cautiously.
boto3==1.42.97
beautifulsoup4==4.14.3
lxml==6.1.0
# pymupdf>=1.26.7 closes GHSA-cxqh-p2w9-fmr7 but requires Python 3.10+.
# The CVE is for `python -m fitz` (CLI path-traversal); we only import
# fitz programmatically, so the exploit path does not apply here. Bump
# the pin when the venv moves off Python 3.9.
pymupdf==1.26.7
python-docx==1.2.0
pyarrow==21.0.0
opensearch-py==2.8.0
sentence-transformers==3.4.1
qdrant-client>=1.11,<2
torch==2.2.2
numpy<2
pymongo==4.17.0
cryptography==48.0.0
